{
  "name": "mirage-engine-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter mirage-engine dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
