{
  "name": "mirage-engine-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter mirage-engine dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
