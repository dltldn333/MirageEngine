::header{pkg="@mirage-engine/core"}

# MirageEngine

> **A core of mirage-engine**

This package does not work alone. Please install [mirage-engine](https://www.npmjs.com/package/mirage-engine) instead.

::BASE
